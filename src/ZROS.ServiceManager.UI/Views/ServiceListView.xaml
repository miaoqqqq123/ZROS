<UserControl x:Class="ZROS.ServiceManager.UI.Views.ServiceListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Toolbar -->
            <RowDefinition Height="*"/>     <!-- List + Detail -->
        </Grid.RowDefinitions>

        <!-- ── Toolbar ── -->
        <Border Grid.Row="0" Style="{StaticResource CardStyle}">
            <StackPanel Orientation="Horizontal">
                <Button Content="▶ Start"   Command="{Binding StartServiceCommand}"   Style="{StaticResource SuccessButtonStyle}" Margin="2"/>
                <Button Content="■ Stop"    Command="{Binding StopServiceCommand}"    Style="{StaticResource DangerButtonStyle}"  Margin="2"/>
                <Button Content="↺ Restart" Command="{Binding RestartServiceCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="2"/>
                <Button Content="▶▶ Start All" Command="{Binding StartAllCommand}"   Style="{StaticResource SuccessButtonStyle}" Margin="2"/>
                <Button Content="■■ Stop All"  Command="{Binding StopAllCommand}"    Style="{StaticResource DangerButtonStyle}"  Margin="2"/>
                <Separator Margin="8,0"/>
                <TextBlock Text="Filter:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                <TextBox Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                         Width="160" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- ── Service List ── -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}">
            <ListView ItemsSource="{Binding Services}"
                      SelectedItem="{Binding SelectedService}"
                      Style="{StaticResource ZrosListViewStyle}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Status" Width="80">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Ellipse Style="{StaticResource StatusIndicatorStyle}"
                                                 Fill="{Binding StateColor}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Name"     DisplayMemberBinding="{Binding Name}"       Width="180"/>
                        <GridViewColumn Header="State"    DisplayMemberBinding="{Binding StateText}"  Width="90"/>
                        <GridViewColumn Header="PID"      DisplayMemberBinding="{Binding ProcessId}"  Width="70"/>
                        <GridViewColumn Header="Memory"   DisplayMemberBinding="{Binding MemoryText}" Width="90"/>
                        <GridViewColumn Header="CPU"      DisplayMemberBinding="{Binding CpuText}"    Width="70"/>
                        <GridViewColumn Header="Restarts" DisplayMemberBinding="{Binding RestartCount}" Width="80"/>
                        <GridViewColumn Header="Uptime"   DisplayMemberBinding="{Binding UptimeText}" Width="100"/>
                    </GridView>
                </ListView.View>

                <!-- Right-click context menu -->
                <ListView.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="▶ Start"   Command="{Binding StartServiceCommand}"/>
                        <MenuItem Header="■ Stop"    Command="{Binding StopServiceCommand}"/>
                        <MenuItem Header="↺ Restart" Command="{Binding RestartServiceCommand}"/>
                    </ContextMenu>
                </ListView.ContextMenu>
            </ListView>
        </Border>
    </Grid>
</UserControl>
