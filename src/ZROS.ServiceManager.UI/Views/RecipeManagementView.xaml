<UserControl x:Class="ZROS.ServiceManager.UI.Views.RecipeManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Style="{StaticResource CardStyle}">
            <StackPanel Orientation="Horizontal">
                <Button Content="📋 New Recipe"       Command="{Binding NewRecipeCommand}"    Style="{StaticResource PrimaryButtonStyle}" Margin="2"/>
                <Button Content="⬇ Load Selected"    Command="{Binding LoadRecipeCommand}"   Style="{StaticResource SuccessButtonStyle}" Margin="2"/>
                <Button Content="💾 Save As Recipe"  Command="{Binding SaveAsRecipeCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="2"/>
                <Button Content="🗑 Delete Selected" Command="{Binding DeleteRecipeCommand}" Style="{StaticResource DangerButtonStyle}"  Margin="2"/>
                <Button Content="↻ Refresh"          Command="{Binding RefreshCommand}"      Style="{StaticResource PrimaryButtonStyle}" Margin="2"/>
            </StackPanel>
        </Border>

        <!-- Recipe List -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}">
            <ListView ItemsSource="{Binding Recipes}"
                      SelectedItem="{Binding SelectedRecipe}"
                      Style="{StaticResource ZrosListViewStyle}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Name"          DisplayMemberBinding="{Binding Name}"          Width="200"/>
                        <GridViewColumn Header="Version"       DisplayMemberBinding="{Binding Version}"       Width="80"/>
                        <GridViewColumn Header="Services"      DisplayMemberBinding="{Binding ServiceCount}"  Width="80"/>
                        <GridViewColumn Header="Description"   DisplayMemberBinding="{Binding Description}"   Width="260"/>
                        <GridViewColumn Header="Last Modified" DisplayMemberBinding="{Binding LastModified}"  Width="160"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>
    </Grid>
</UserControl>
